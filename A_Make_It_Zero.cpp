#include <bits/stdc++.h>
using namespace std;

void solve(){
    int n ;
    cin >> n ;

    int arr[n];
    bool zero = false ; ; 
    for(int i = 0 ; i < n ; i++){
        cin >> arr[i];
        if(arr[i] == 0){
            zero = true ;
        }
    }

    
}

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;
    while(t--){
        solve();
    }
return 0 ;
}